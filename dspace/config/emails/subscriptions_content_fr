## Courriel envoyé à l'adresse désignée concernant les mises à jour des éléments abonnés
##
## Paramètres : {0} Mises à jour des communautés
##             {1} Mises à jour des collections
#set($subject = "${config.get('dspace.name')} Abonnements")

Ce courriel est envoyé par ${config.get('dspace.name')} en fonction des préférences d'abonnement choisies.

#if( not( "$params[0]" == "" ))
Abonnements aux communautés :
-----------------------------
Liste des éléments modifiés : ${params[0]}

#end
#if( not( "$params[1]" == "" ))
Abonnements aux collections :
-----------------------------
Liste des éléments modifiés : ${params[1]}
#end
